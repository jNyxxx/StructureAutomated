import { PendingBackendPage } from "@/components/layout/pending-backend-page";

export default function ImportCsvPage() {
  return (
    <PendingBackendPage
      title="Import CSV"
      description="CSV import routing is available for design validation only. Import, validation, and persistence APIs are not mounted yet."
    />
  );
}

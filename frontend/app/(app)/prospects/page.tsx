import { PendingBackendPage } from "@/components/layout/pending-backend-page";

export default function ProspectsPage() {
  return (
    <PendingBackendPage
      title="Prospects"
      description="Prospect list navigation is ready for the local/mock MVP shell. Contacts/prospects HTTP APIs are not mounted yet."
    />
  );
}

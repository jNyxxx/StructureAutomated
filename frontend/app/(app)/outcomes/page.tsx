import { PendingBackendPage } from "@/components/layout/pending-backend-page";

export default function OutcomesPage() {
  return (
    <PendingBackendPage
      title="Outcomes"
      description="Outcomes and ROI navigation is present. Charts and metrics stay pending until outcome HTTP APIs are mounted."
    />
  );
}

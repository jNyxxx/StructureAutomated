export default function BillingPage() {
  return (
    <section>
      <h1 className="text-xl font-semibold">Billing</h1>
      <p className="mt-2 text-sm">Billing state is mock-only for the MVP and wired in a later slice.</p>
    </section>
  );
}

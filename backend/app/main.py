"""FastAPI application entrypoint (placeholder).

Implemented in Phase 0, Slice 3 (backend foundation: config, structured logging,
request IDs, standard error envelope, health/ready/live endpoints). Intentionally
empty in Slice 1 — no application behaviour is defined here yet.
"""
